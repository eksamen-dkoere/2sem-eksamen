---
//@ts-nocheck
import Layout from '../layouts/Layout.astro';
import Welcome from '../components/Welcome.astro';
import Hero from '../components/Hero.astro';
import Footer from '../components/Footer.astro'
import Countdown from '../components/Countdown.astro';
import Card from '../components/Card.astro';
import Breadcrumbs from '../components/Breadcrumbs.astro';
import Card from '../components/Card.astro';

import '../styles/effects.css';
import '../styles/global.css';
import '../styles/reset.css';
import '../styles/typography.css';
import '../styles/variables.css';
import '../../public/images/luftballon.webp';

export async function getStaticPaths() {
  const endpoint =
    "https://tkndfhivujdlyksacqma.supabase.co/rest/v1/Dankort-oeremaerket?select=*";
  const key = "sb_publishable_MNgNlIAwa1Fk9Ub05gnGTw_IhTr0m_G";

  const response = await fetch(endpoint, { headers: { apikey: key } });
  const products = await response.json();

  return products.map((product) => ({
    params: { slug: product.id.toString() },
    props: { product },
  }));
}
---

<Layout>

<Breadcrumbs />
  
<section class="product_grid">
    <div>
        <Countdown />
        <img src="/images/luftballon.svg" alt="luftballon">
    </div>
    <div class="tekst_grid">
        <div>
        <h1>Dankort Øremærket (indsæt))</h1>
        <h2>(Pris),-</h2>
        </div>
        <div>
        <h3>Størrelse</h3>
        <!-- Drop down her med størrelser -->
         </div>
            <div class="oplysninger">
                <div>
                <h4>Produktoplysninger</h4>
                 </div>
                 <div>
                 <h5>Materiale</h5>
                 <p>100% genanvendelige materialer</p>
                 </div>
                <div>
                 <h5>Product type</h5>
                 <p>(Indsæt product type)</p>
                  </div>
                 <div>
                 <h5>Beskrivelse</h5>
                 <p>Dette produkt er et limited edition produkt lavet som en del af vores Dankort Øremærket kampagne, og 100% af overskuddet går til Den Danske Naturfond.</p>
                </div>
            </div>
    </div>
</section>

</Layout>

<!-- <Layout title={product.title}>
  <main>
    <section class="product_grid">
      <div class="gallery">
        <div class="main-img-container">
          <img src={product.image} alt={product.title} id="main-img" />
        </div>
      </div>
      <div>
       
          <a href="/">FORSIDE</a> / <a href="../list">ALLE ØL</a> / {
            product.title
          }
      
        <h1>{product.title}</h1>
        <div class="purchase-options">
          <div class="option-card">
    
            <h2 class="price-stk"><strong>{product.price},-</strong></h2>
          </div>

        </div>
       
        <DetailsProduct data={product} />
      </div>
    </section>
  </Layout> -->


<style>

  .product_grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    max-width: 760px;
    margin: 0 auto;
  }

  .tekst_grid {
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }
  
  .oplysninger {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  /* Desktop version (Skærme over 768px) */
  @media (min-width: 768px) {
    .product_grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    max-width: 760px;
    margin: 0 auto;
    padding: 5rem;
  }
  }
</style>